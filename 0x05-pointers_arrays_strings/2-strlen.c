#include "main.h"

/**
* _strlen - A function that returns the length of a string.
*@s: String to check
* Return: lengh
*/
int _strlen(char *s)
{
int lengh = 0;

while (s[lengh] != '\0')
{
lengh++
}
return (lengh);
}
