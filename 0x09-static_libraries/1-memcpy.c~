#include <stdio.h>

#include "main.h"
/**
 * main - prints the name of the program
 * @dest: number of argv array
 * @src: array of arguments
 * @n: is n
 * Return: Always 0 (Success)
 */


char *_memcpy(char *dest, char *src, unsigned int n);
{
return (0);
}
