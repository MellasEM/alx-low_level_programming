#include <stdio.h>

#include "main.h"
/**
 * main - prints the name of the program
 * @accept: number of argv array
 * @s: array of arguments
 * Return: Always 0 (Success)
 */
unsigned int _strspn(char *s, char *accept);
{
return (0);
}
