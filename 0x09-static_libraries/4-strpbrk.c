#include <stdio.h>

#include "main.h"
/**
 * main - prints the name of the program
 * @s: number of argv array
 * @accept: array of arguments
 * Return: Always 0 (Success)
 */
char *_strpbrk(char *s, char *accept)
{
return (0);
}
