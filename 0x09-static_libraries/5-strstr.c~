#include <stdio.h>

#include "main.h"
/**
 * main - prints the name of the program
 * @haystack: number of argv array
 * @needle: array of arguments
 * Return: Always 0 (Success)
 */
char *_strstr(char *haystack, char *needle);

{
return (0);
}
