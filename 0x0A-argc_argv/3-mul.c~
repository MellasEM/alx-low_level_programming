#include <stdio.h>
#include "main.h"
#include <stdlib.h>

int main(int argc, char *argv[])
{
int n1 = atoi(argv[1]);
int n2 = atoi(argv[2]);
if (argc != 3)
{
printf("Error\n");
return (1);
}
printf("%d\n", n1 * n2);
return (0);
}
